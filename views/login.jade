h2 Sign in to your account

- if ('undefined' !== typeof errors && errors.length)
  ul#errors
    - each error in errors
      li.error= error
form(action='/login', method='POST')
  #login
    input(type='email', name=everyauth.password.loginFormFieldName, value=email, placeholder='Email address', autofocus='autofocus')
  #password
    input(type='password', name=everyauth.password.passwordFormFieldName, placeholder='Password')
  #submit
    input(type='submit', value='Sign in')

a(href='/forgot') Forgot password?
