h2 Register for a new account

- if ('undefined' !== typeof errors && errors.length)
  ul#errors
    - each error in errors
      li.error= error
form(action='/register', method='POST')
  #login
    input(type='email', name=everyauth.password.loginFormFieldName, value=userParams[everyauth.password.loginFormFieldName], placeholder='Email address', autofocus='autofocus')
  #password
    input(type='password', name=everyauth.password.passwordFormFieldName, placeholder='Password')
  #submit
    input(type='submit', value='Register')

a(href='/login') Already have an account?
